
# 🤖 Правила Cursor AI - Telegram Bot Development

**Cursor AI: Помни — UX важнее автоматизации! Пользователь должен контролировать навигацию!**

## 🚨 КРИТИЧЕСКИ ВАЖНО
**Telegram боты - это НЕ веб-приложения!** Не применяй стандартные веб-практики безопасности.

## ✅ Telegram сам обеспечивает:
- Аутентификацию пользователей
- CSRF защиту
- XSS защиту
- Rate limiting
- Управление сессиями

## 🚫 НЕ используй для Telegram ботов:
- `SESSION_SECRET` - не нужен
- `JWT токены` - Telegram ID уже идентификатор
- `CSRF токены` - нет веб-форм
- `Пароли пользователей` - Telegram управляет аутентификацией
- `Веб-сессии` - Telegram управляет сессиями

## 🔧 Обязательные правила:

### 1. Webhook
```javascript
// ✅ Правильно
await bot.setWebhook(`${process.env.WEBHOOK_URL}/webhook`);

// ❌ Неправильно
bot.setWebhookInfo(); // НЕ СУЩЕСТВУЕТ
```

### 2. Клавиатуры - КРИТИЧЕСКИ ВАЖНО
```javascript
// ❌ НЕ смешивай inline и reply клавиатуры
// ✅ Отправляй отдельными сообщениями

// Сначала отправляем inline-клавиатуру
await ctx.reply('Выберите действие:', {
  reply_markup: inlineKeyboard
});

// Затем отдельно отправляем reply-клавиатуру
await ctx.reply('Используйте кнопки ниже:', {
  reply_markup: replyKeyboard
});
```

#### 2.1. Длина текста кнопок - КРИТИЧЕСКИ ВАЖНО
```javascript
// ❌ НЕ ДОПУСКАЙ обрезания текста кнопок Telegram'ом
// ✅ Максимальная длина текста кнопки: 20 символов

// ❌ Неправильно - текст обрежется
{ text: '📊 Детальная статистика рефералов', callback_data: 'stats' }

// ✅ Правильно - короткий текст
{ text: '📊 Статистика', callback_data: 'stats' }

// ✅ Правильно - очень короткий текст
{ text: '📈 По уровням', callback_data: 'earnings_by_level' }

// ✅ Правильно - используй эмодзи для экономии места
{ text: '💰 Вывод', callback_data: 'withdrawal' }
```

### 3. Команда /start - КРИТИЧЕСКИ ВАЖНО

**ВАЖНО: В проекте должен быть только один обработчик команды /start на одного бота. Если есть несколько — будет конфликт, и сработает только последний зарегистрированный! Не допускайте регистрации нескольких обработчиков /start в разных модулях.**

- Всегда должен быть только один обработчик /start на одного бота.
- Обработка /start должна быть реализована так, чтобы команда гарантированно срабатывала в любом состоянии пользователя (даже если пользователь находится в FSM-сцене или другой middleware).
- Рекомендуется реализовывать обработку /start на самом верхнем уровне (middleware ДО stage.middleware()), с принудительным выходом из всех сцен и сбросом состояния.

```javascript
// ✅ ОБЯЗАТЕЛЬНО: принудительный выход из FSM при /start
bot.use(async (ctx, next) => {
  if (ctx.message && typeof ctx.message.text === 'string' && ctx.message.text.startsWith('/start')) {
    // Принудительно выходим из всех сцен
    if (ctx.scene && ctx.scene.current) {
      try {
        await ctx.scene.leave();
        console.log('[webhook] Пользователь вышел из сцены при команде /start');
      } catch (error) {
        console.log('[webhook] Ошибка при выходе из сцены:', error);
      }
    }
    // Сбрасываем сессию
    if (ctx.session) {
      ctx.session = {};
    }
    await handleStart(ctx); // handleStart — ваша функция старта
    return;
  }
  return next();
});
```

- Не допускайте регистрации нескольких обработчиков /start в разных модулях — это приведёт к конфликту и неработающему боту!
- Проверяйте, что /start всегда работает после любых изменений FSM, middleware и сцен.

### 4. Безопасность
```javascript
// Проверка администратора
const isAdmin = msg.from.id.toString() === process.env.ADMIN_USER_ID;

// Параметризованные SQL запросы
await db.query('SELECT * FROM users WHERE telegram_id = $1', [msg.from.id]);

// Обработка ошибок
try {
    await processRequest(msg);
} catch (error) {
    logger.error('Ошибка:', error);
    await bot.sendMessage(chatId, '❌ Произошла ошибка');
}
```

### 5. Обработка ошибок - ОБЯЗАТЕЛЬНО
```javascript
// ✅ ВСЕГДА выводим детали ошибки в консоль
try {
    await riskyOperation();
} catch (error) {
    console.error('[ModuleName] Ошибка при выполнении операции:', error);
    console.error('[ModuleName] Детали ошибки:', error instanceof Error ? error.stack : error);
    // Fallback логика
}
```

### 6. Переменные окружения
```env
TELEGRAM_BOT_TOKEN=your_token
ADMIN_USER_ID=your_telegram_id
DATABASE_URL=postgresql://...
WEBHOOK_URL=https://your-domain.com

# НЕ НУЖНЫ для Telegram ботов:
# JWT_SECRET - Telegram сам обеспечивает безопасность
# SESSION_SECRET - не нужен
```

## 🚨 Частые ошибки:
1. **Смешивание клавиатур** - отправляй отдельно
2. **Несуществующие методы** - проверяй документацию
3. **Игнорирование ошибок** - всегда используй try-catch
4. **Хардкод токенов** - используй переменные окружения
5. **Дублирование команд** - одна команда = один обработчик
6. **Отсутствие выхода из FSM при /start** - команда может не работать
7. **Длинный текст кнопок** - Telegram обрезает текст, максимум 20 символов

## 📋 Чек-лист:
- [ ] Webhook настроен в Telegram API
- [ ] Клавиатуры отправляются отдельно
- [ ] Текст кнопок не превышает 20 символов
- [ ] Все ошибки обрабатываются с выводом в консоль
- [ ] SQL запросы параметризованы
- [ ] Токены в переменных окружения
- [ ] Команда /start принудительно выходит из FSM
- [ ] Нет дублирования команд
- [ ] Подробное логирование входящих апдейтов

## 🚫 ЗАПРЕЩЕНО БЕЗ РАЗРЕШЕНИЯ:
- **Изменять UX/UI** - не добавлять новые сообщения, подсказки, кнопки
- **Менять логику навигации** - не изменять существующие меню и переходы
- **Добавлять функциональность** - не расширять возможности без запроса
- **Менять тексты** - не изменять существующие сообщения и описания

## ✅ Разрешено ТОЛЬКО:
- Исправление багов и ошибок
- Рефакторинг кода без изменения поведения
- Добавление функционала по явному запросу пользователя
- Миграция существующего функционала из v1 в v2

## 🔍 ПРОВЕРКА ПЕРЕД КОММИТОМ:
1. **Синтаксис корректен** - `npm run lint:check`
2. **Безопасность соблюдена** - нет смешивания клавиатур
3. **Производительность оптимальна** - клавиатуры разделены
4. **Код читаем и поддерживаем** - нет дублирования
5. **Стандарты соблюдены** - следуем правилам Telegram
6. **Ошибки обработаны** - все catch блоки выводят детали
7. **Команда /start работает** - принудительный выход из FSM

---

**Помни: Telegram сам обеспечивает безопасность - не дублируй!**
**Помни: UX/UI изменяется ТОЛЬКО по разрешению пользователя!**
**Помни: Клавиатуры НИКОГДА не смешиваются!**
**Помни: Команда /start ВСЕГДА должна работать!**

