---
alwaysApply: true
---

# 🔒 Безопасность Telegram ботов

## 🚨 КРИТИЧЕСКИ ВАЖНО

**Telegram боты - это НЕ веб-приложения!** Не применяй стандартные веб-практики безопасности.

## ✅ Что Telegram обеспечивает автоматически

- **Аутентификация пользователей** - через Telegram ID
- **CSRF защита** - не нужна для ботов
- **XSS защита** - Telegram API безопасен
- **Rate limiting** - встроенный в API
- **Управление сессиями** - Telegram управляет

## 🔧 Правильная реализация безопасности

### 1. Проверка администратора
```typescript
// ✅ Правильно
const isAdmin = msg.from.id.toString() === process.env.ADMIN_USER_ID;

// ❌ Неправильно
const isAdmin = msg.from.username === 'admin'; // Небезопасно
```

### 2. Переменные окружения
```env
# ✅ Обязательные переменные
BOT_TOKEN=your_bot_token
ADMIN_USER_ID=your_telegram_id
DATABASE_URL=postgresql://...
WEBHOOK_URL=https://your-domain.com

# ❌ НЕ нужны для Telegram ботов
SESSION_SECRET=          # НЕ НУЖЕН
JWT_SECRET=             # НЕ НУЖЕН
CSRF_SECRET=            # НЕ НУЖЕН
```

### 3. Обработка ошибок
```typescript
// ✅ Всегда используй try-catch
try {
    await processRequest(msg);
} catch (error) {
    this.logger.error('Ошибка:', error);
    await bot.sendMessage(chatId, '❌ Произошла ошибка');
}
```

## 🚫 НЕ используй для Telegram ботов

- `SESSION_SECRET` - не нужен
- `JWT токены` - Telegram ID уже идентификатор
- `CSRF токены` - нет веб-форм
- `Пароли пользователей` - Telegram управляет аутентификацией
- `Веб-сессии` - Telegram управляет сессиями

## 🛡️ Дополнительные меры безопасности

### 1. Валидация входных данных
```typescript
// ✅ Проверяй данные от пользователя
if (!msg.text || msg.text.length > 4096) {
    await bot.sendMessage(chatId, '❌ Недопустимое сообщение');
    return;
}
```

### 2. Логирование действий
```typescript
// ✅ Логируй важные действия
this.logger.info(`Пользователь ${msg.from.id} выполнил действие: ${action}`);
```

### 3. Ограничение доступа
```typescript
// ✅ Ограничивай доступ к функциям
const allowedUsers = process.env.ALLOWED_USERS?.split(',') || [];
if (!allowedUsers.includes(msg.from.id.toString())) {
    await bot.sendMessage(chatId, '❌ Доступ запрещен');
    return;
}
```

## 📋 Чек-лист безопасности

- [ ] Токен бота в переменных окружения
- [ ] Проверка администратора по Telegram ID
- [ ] Все ошибки обрабатываются в try-catch
- [ ] Валидация пользовательского ввода
- [ ] Логирование важных действий
- [ ] НЕТ веб-практик безопасности

## 🔍 Частые ошибки безопасности

### 1. Хардкод токенов
```typescript
// ❌ НЕПРАВИЛЬНО
const token = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz';

// ✅ ПРАВИЛЬНО
const token = process.env.BOT_TOKEN;
```

### 2. Игнорирование ошибок
```typescript
// ❌ НЕПРАВИЛЬНО
bot.on('message', async (msg) => {
    await processMessage(msg); // Может упасть
});

// ✅ ПРАВИЛЬНО
bot.on('message', async (msg) => {
    try {
        await processMessage(msg);
    } catch (error) {
        this.logger.error('Ошибка обработки сообщения:', error);
        await bot.sendMessage(msg.chat.id, '❌ Произошла ошибка');
    }
});
```

### 3. Небезопасная проверка прав
```typescript
// ❌ НЕПРАВИЛЬНО
if (msg.from.username === 'admin') {
    // Легко подделать username
}

// ✅ ПРАВИЛЬНО
if (msg.from.id.toString() === process.env.ADMIN_USER_ID) {
    // Telegram ID нельзя подделать
}
```

## 📚 Дополнительные ресурсы

- [Telegram Bot Security](https://core.telegram.org/bots/security)
- [Bot API Security](https://core.telegram.org/bots/api#security-considerations)
- [Best Practices for Bots](https://core.telegram.org/bots/best-practices)
# 🔒 Безопасность Telegram ботов

## 🚨 КРИТИЧЕСКИ ВАЖНО

**Telegram боты - это НЕ веб-приложения!** Не применяй стандартные веб-практики безопасности.

## ✅ Что Telegram обеспечивает автоматически

- **Аутентификация пользователей** - через Telegram ID
- **CSRF защита** - не нужна для ботов
- **XSS защита** - Telegram API безопасен
- **Rate limiting** - встроенный в API
- **Управление сессиями** - Telegram управляет

## 🔧 Правильная реализация безопасности

### 1. Проверка администратора
```typescript
// ✅ Правильно
const isAdmin = msg.from.id.toString() === process.env.ADMIN_USER_ID;

// ❌ Неправильно
const isAdmin = msg.from.username === 'admin'; // Небезопасно
```

### 2. Переменные окружения
```env
# ✅ Обязательные переменные
BOT_TOKEN=your_bot_token
ADMIN_USER_ID=your_telegram_id
DATABASE_URL=postgresql://...
WEBHOOK_URL=https://your-domain.com

# ❌ НЕ нужны для Telegram ботов
SESSION_SECRET=          # НЕ НУЖЕН
JWT_SECRET=             # НЕ НУЖЕН
CSRF_SECRET=            # НЕ НУЖЕН
```

### 3. Обработка ошибок
```typescript
// ✅ Всегда используй try-catch
try {
    await processRequest(msg);
} catch (error) {
    this.logger.error('Ошибка:', error);
    await bot.sendMessage(chatId, '❌ Произошла ошибка');
}
```

## 🚫 НЕ используй для Telegram ботов

- `SESSION_SECRET` - не нужен
- `JWT токены` - Telegram ID уже идентификатор
- `CSRF токены` - нет веб-форм
- `Пароли пользователей` - Telegram управляет аутентификацией
- `Веб-сессии` - Telegram управляет сессиями

## 🛡️ Дополнительные меры безопасности

### 1. Валидация входных данных
```typescript
// ✅ Проверяй данные от пользователя
if (!msg.text || msg.text.length > 4096) {
    await bot.sendMessage(chatId, '❌ Недопустимое сообщение');
    return;
}
```

### 2. Логирование действий
```typescript
// ✅ Логируй важные действия
this.logger.info(`Пользователь ${msg.from.id} выполнил действие: ${action}`);
```

### 3. Ограничение доступа
```typescript
// ✅ Ограничивай доступ к функциям
const allowedUsers = process.env.ALLOWED_USERS?.split(',') || [];
if (!allowedUsers.includes(msg.from.id.toString())) {
    await bot.sendMessage(chatId, '❌ Доступ запрещен');
    return;
}
```

## 📋 Чек-лист безопасности

- [ ] Токен бота в переменных окружения
- [ ] Проверка администратора по Telegram ID
- [ ] Все ошибки обрабатываются в try-catch
- [ ] Валидация пользовательского ввода
- [ ] Логирование важных действий
- [ ] НЕТ веб-практик безопасности

## 🔍 Частые ошибки безопасности

### 1. Хардкод токенов
```typescript
// ❌ НЕПРАВИЛЬНО
const token = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz';

// ✅ ПРАВИЛЬНО
const token = process.env.BOT_TOKEN;
```

### 2. Игнорирование ошибок
```typescript
// ❌ НЕПРАВИЛЬНО
bot.on('message', async (msg) => {
    await processMessage(msg); // Может упасть
});

// ✅ ПРАВИЛЬНО
bot.on('message', async (msg) => {
    try {
        await processMessage(msg);
    } catch (error) {
        this.logger.error('Ошибка обработки сообщения:', error);
        await bot.sendMessage(msg.chat.id, '❌ Произошла ошибка');
    }
});
```

### 3. Небезопасная проверка прав
```typescript
// ❌ НЕПРАВИЛЬНО
if (msg.from.username === 'admin') {
    // Легко подделать username
}

// ✅ ПРАВИЛЬНО
if (msg.from.id.toString() === process.env.ADMIN_USER_ID) {
    // Telegram ID нельзя подделать
}
```

## 📚 Дополнительные ресурсы

- [Telegram Bot Security](https://core.telegram.org/bots/security)
- [Bot API Security](https://core.telegram.org/bots/api#security-considerations)
- [Best Practices for Bots](https://core.telegram.org/bots/best-practices)
