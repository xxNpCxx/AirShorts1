# 🚫 TypeScript: Избегание any типов

## 🎯 Основная цель
**НИКОГДА не используйте `any` типы!** Вместо этого используйте правильную типизацию TypeScript для обеспечения безопасности типов.

## ❌ ЗАПРЕЩЕНО
```typescript
// ❌ НЕПРАВИЛЬНО - никогда не используйте any
function processData(data: any): any {
  return data.someProperty;
}

const result: any = getData();
const ctx: any = context;
```

## ✅ ПРАВИЛЬНО
```typescript
// ✅ ПРАВИЛЬНО - используйте конкретные типы
interface DataType {
  someProperty: string;
  count: number;
}

function processData(data: DataType): string {
  return data.someProperty;
}

const result: DataType = getData();
const ctx: Context<Update> = context;
```

## 🔧 Стратегии замены any

### 1. **Интерфейсы и типы**
```typescript
// ❌ Плохо
function handleMessage(msg: any) {
  console.log(msg.text);
}

// ✅ Хорошо
interface TelegramMessage {
  text: string;
  from?: { id: number; username?: string };
}

function handleMessage(msg: TelegramMessage) {
  console.log(msg.text);
}
```

### 2. **Union типы**
```typescript
// ❌ Плохо
function processInput(input: any) {
  if (typeof input === 'string') return input.toUpperCase();
  if (typeof input === 'number') return input * 2;
  return null;
}

// ✅ Хорошо
function processInput(input: string | number): string | number | null {
  if (typeof input === 'string') return input.toUpperCase();
  if (typeof input === 'number') return input * 2;
  return null;
}
```

### 3. **Generic типы**
```typescript
// ❌ Плохо
function createArray(item: any, count: number): any[] {
  return Array(count).fill(item);
}

// ✅ Хорошо
function createArray<T>(item: T, count: number): T[] {
  return Array(count).fill(item);
}
```

### 4. **Type guards**
```typescript
// ❌ Плохо
function processData(data: any) {
  return data.value + 10;
}

// ✅ Хорошо
interface DataWithValue {
  value: number;
}

function isDataWithValue(data: unknown): data is DataWithValue {
  return typeof data === 'object' && data !== null && 'value' in data;
}

function processData(data: unknown) {
  if (isDataWithValue(data)) {
    return data.value + 10;
  }
  throw new Error('Invalid data format');
}
```

## 🎭 Работа с внешними API

### **API ответы**
```typescript
// ❌ Плохо
async function fetchData(): Promise<any> {
  const response = await fetch('/api/data');
  return response.json();
}

// ✅ Хорошо
interface ApiResponse {
  data: UserData[];
  total: number;
  page: number;
}

async function fetchData(): Promise<ApiResponse> {
  const response = await fetch('/api/data');
  return response.json();
}
```

### **Event handlers**
```typescript
// ❌ Плохо
function handleClick(event: any) {
  console.log(event.target.value);
}

// ✅ Хорошо
function handleClick(event: React.MouseEvent<HTMLButtonElement>) {
  console.log(event.currentTarget.value);
}
```

## 🔍 Когда можно использовать any (крайне редко!)

### **Только в этих случаях:**
1. **Миграция с JavaScript** - временно, с TODO комментарием
2. **Внешние библиотеки без типов** - используйте `unknown` вместо `any`
3. **Тестирование** - только в тестах, не в продакшн коде

```typescript
// ✅ Разрешено только с TODO и планом исправления
// TODO: Заменить any на правильный тип после рефакторинга
function legacyFunction(data: any): any {
  return processLegacyData(data);
}

// ✅ Для внешних библиотек используйте unknown
function processExternalData(data: unknown): string {
  if (typeof data === 'string') return data;
  if (typeof data === 'object' && data !== null && 'value' in data) {
    return String((data as { value: unknown }).value);
  }
  return 'default';
}
```

## 🛠️ Инструменты для избегания any

### **TypeScript Compiler Options**
```json
{
  "compilerOptions": {
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "noImplicitReturns": true
  }
}
```

### **ESLint правила**
```javascript
// eslint.config.js
rules: {
  '@typescript-eslint/no-explicit-any': 'error',
  '@typescript-eslint/no-unsafe-assignment': 'error',
  '@typescript-eslint/no-unsafe-member-access': 'error',
  '@typescript-eslint/no-unsafe-call': 'error'
}
```

## 📋 Чек-лист перед коммитом

- [ ] Нет `any` типов в коде
- [ ] Все функции имеют типы параметров и возвращаемых значений
- [ ] Все переменные имеют явные типы или выводятся TypeScript
- [ ] Используются интерфейсы для объектов
- [ ] Используются union типы для альтернативных значений
- [ ] Используются generic типы где это уместно
- [ ] TypeScript компилируется без ошибок
- [ ] ESLint не показывает ошибки типизации

## 🚨 Исключения и альтернативы

### **Вместо `any` используйте:**

| ❌ Плохо | ✅ Хорошо | Описание |
|-----------|-----------|----------|
| `any` | `unknown` | Неизвестный тип, требует проверки |
| `any` | `Record<string, unknown>` | Объект с неизвестными ключами |
| `any` | `never` | Значение, которое никогда не произойдет |
| `any` | `object` | Любой объект (но не примитив) |

## 💡 Советы по рефакторингу

1. **Начните с интерфейсов** - опишите структуру данных
2. **Используйте type guards** - проверяйте типы во время выполнения
3. **Применяйте union типы** - для альтернативных значений
4. **Создавайте generic функции** - для переиспользуемого кода
5. **Тестируйте типы** - убедитесь, что TypeScript понимает ваш код

## 🎯 Примеры рефакторинга

### **До (с any):**
```typescript
function processUser(user: any) {
  if (user.name && user.age) {
    return `${user.name} is ${user.age} years old`;
  }
  return 'Unknown user';
}
```

### **После (без any):**
```typescript
interface User {
  name: string;
  age: number;
}

function processUser(user: User | null | undefined): string {
  if (user?.name && user?.age) {
    return `${user.name} is ${user.age} years old`;
  }
  return 'Unknown user';
}
```

---

**Помните: `any` - это отказ от всех преимуществ TypeScript! Используйте правильную типизацию для безопасного и предсказуемого кода.** 🚀
description:
globs:
alwaysApply: true
---
