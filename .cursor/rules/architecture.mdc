# 🏗️ Архитектура Telegram ботов на NestJS

## 🎯 Основные принципы

- **Модульность** - разделяй функциональность на модули
- **Разделение ответственности** - каждый модуль отвечает за свою область
- **Легкость тестирования** - код должен быть легко тестируемым
- **Масштабируемость** - архитектура должна расти с проектом

## 🏛️ Рекомендуемая структура проекта

```
src/
├── app.module.ts              # Главный модуль приложения
├── main.ts                    # Точка входа
├── bot/                       # Основная логика бота
│   ├── bot.update.ts          # Обработчики сообщений
│   └── bot.service.ts         # Сервис бота
├── updates/                   # Обработчики обновлений
│   ├── bot.update.ts          # Основные обработчики
│   └── menu.update.ts         # Обработчики меню
├── services/                  # Бизнес-логика
│   ├── users/                 # Работа с пользователями
│   ├── settings/              # Настройки
│   ├── keyboards/             # Клавиатуры
│   └── menu/                  # Меню
├── database/                  # Работа с базой данных
│   ├── database.module.ts     # Модуль БД
│   └── migrate.ts             # Миграции
├── redis/                     # Redis для кэширования
│   └── redis.module.ts        # Модуль Redis
├── logger/                    # Логирование
│   └── logger.module.ts       # Модуль логгера
└── config/                    # Конфигурация
    └── configuration.ts        # Настройки приложения
```

## 🔧 Паттерны архитектуры

### 1. Модульная структура
```typescript
// ✅ Правильно - модульная структура
@Module({
  imports: [DatabaseModule, LoggerModule, RedisModule],
  providers: [BotUpdate, MenuUpdate],
  controllers: [HealthController],
})
export class AppModule {}
```

### 2. Разделение обработчиков
```typescript
// ✅ Правильно - отдельные обработчики
@Update()
export class BotUpdate {
  @Start()
  async startCommand(@Ctx() ctx: Context) {
    // Обработка команды /start
  }
  
  @On('text')
  async handleText(@Ctx() ctx: Context) {
    // Обработка текстовых сообщений
  }
}

@Update()
export class MenuUpdate {
  @Action('menu')
  async handleMenu(@Ctx() ctx: Context) {
    // Обработка меню
  }
}
```

### 3. Сервисный слой
```typescript
// ✅ Правильно - бизнес-логика в сервисах
@Injectable()
export class UsersService {
  async createUser(telegramId: number, username: string) {
    // Логика создания пользователя
  }
  
  async getUser(telegramId: number) {
    // Логика получения пользователя
  }
}
```

## 🚫 Архитектурные антипаттерны

### 1. Монолитная структура
```typescript
// ❌ НЕПРАВИЛЬНО - все в одном файле
@Update()
export class BotUpdate {
  @Start()
  async startCommand(@Ctx() ctx: Context) {
    // 100+ строк кода в одном методе
  }
}
```

### 2. Смешивание слоев
```typescript
// ❌ НЕПРАВИЛЬНО - бизнес-логика в обработчике
@On('text')
async handleText(@Ctx() ctx: Context) {
  // Прямые SQL запросы в обработчике
  const user = await this.db.query('SELECT * FROM users WHERE id = $1', [ctx.from.id]);
  
  // Бизнес-логика в обработчике
  if (user.balance < 100) {
    // Логика проверки баланса
  }
}
```

### 3. Хардкод конфигурации
```typescript
// ❌ НЕПРАВИЛЬНО - конфигурация в коде
const DATABASE_URL = 'postgresql://user:pass@localhost:5432/bot';
const ADMIN_ID = 123456789;
```

## ✅ Правильная архитектура

### 1. Конфигурация через переменные окружения
```typescript
// ✅ Правильно - конфигурация вынесена
@Injectable()
export class ConfigService {
  get databaseUrl(): string {
    return process.env.DATABASE_URL;
  }
  
  get adminId(): string {
    return process.env.ADMIN_USER_ID;
  }
  
  get webhookUrl(): string {
    return process.env.WEBHOOK_URL;
  }
}
```

### 2. Разделение ответственности
```typescript
// ✅ Правильно - каждый сервис отвечает за свою область
@Injectable()
export class MenuService {
  constructor(
    private userService: UsersService,
    private keyboardService: KeyboardsService,
  ) {}
  
  async getMainMenu(userId: number) {
    const user = await this.userService.getUser(userId);
    const keyboard = await this.keyboardService.getMainMenu(user);
    
    return { user, keyboard };
  }
}
```

### 3. Dependency Injection
```typescript
// ✅ Правильно - зависимости инжектируются
@Injectable()
export class BotService {
  constructor(
    private menuService: MenuService,
    private userService: UsersService,
    private logger: LoggerService,
  ) {}
}
```

## 🔄 Жизненный цикл сообщения

### 1. Получение сообщения
```typescript
@On('text')
async handleText(@Ctx() ctx: Context) {
  try {
    await this.messageHandler.handle(ctx);
  } catch (error) {
    this.logger.error('Ошибка обработки сообщения:', error);
  }
}
```

### 2. Валидация и авторизация
```typescript
async handle(ctx: Context) {
  // Валидация
  if (!this.isValidMessage(ctx.message)) {
    return;
  }
  
  // Авторизация
  if (!this.isAuthorized(ctx.from.id)) {
    return;
  }
  
  // Обработка
  await this.processMessage(ctx);
}
```

### 3. Обработка и ответ
```typescript
async processMessage(ctx: Context) {
  const result = await this.menuService.process(ctx.message);
  const keyboard = await this.keyboardService.getKeyboard(result.user);
  
  await ctx.reply(result.response, {
    reply_markup: keyboard,
  });
}
```

## 📋 Чек-лист архитектуры

- [ ] Модульная структура проекта
- [ ] Разделение на слои (updates, services, database)
- [ ] Dependency Injection для сервисов
- [ ] Конфигурация через переменные окружения
- [ ] Обработка ошибок на всех уровнях
- [ ] Логирование важных операций
- [ ] Легкость тестирования компонентов

## 🚀 Масштабирование

### 1. Горизонтальное масштабирование
```typescript
// Поддержка нескольких экземпляров бота
@Module({
  imports: [
    TelegrafModule.forRoot({
      token: process.env.BOT_TOKEN,
      launchOptions: {
        webhook: {
          domain: process.env.WEBHOOK_URL,
          hookPath: '/webhook',
        },
      },
    }),
  ],
})
export class BotModule {}
```

### 2. Вертикальное масштабирование
```typescript
// Разделение функциональности на микросервисы
@Module({
  imports: [UsersModule, SettingsModule, KeyboardsModule, MenuModule],
})
export class AppModule {}
```

### 3. Кэширование
```typescript
// Кэширование часто используемых данных
@Injectable()
export class CacheService {
  async getUser(telegramId: number) {
    const cached = await this.redis.get(`user:${telegramId}`);
    if (cached) return JSON.parse(cached);
    
    const user = await this.userRepository.findById(telegramId);
    await this.redis.setex(`user:${telegramId}`, 3600, JSON.stringify(user));
    return user;
  }
}
```

## 🔧 Webhook настройка

### 1. Правильная конфигурация
```typescript
// ✅ Правильно - webhook настроен
TelegrafModule.forRoot({
  token: process.env.BOT_TOKEN,
  launchOptions: {
    webhook: {
      domain: process.env.WEBHOOK_URL,
      hookPath: '/webhook',
    },
  },
})
```

### 2. Проверка webhook
```typescript
// ✅ Проверяй статус webhook
async checkWebhook() {
  const webhookInfo = await this.bot.telegram.getWebhookInfo();
  if (webhookInfo.url !== `${process.env.WEBHOOK_URL}/webhook`) {
    await this.bot.telegram.setWebhook(`${process.env.WEBHOOK_URL}/webhook`);
  }
}
```
description:
globs:
alwaysApply: false
---
