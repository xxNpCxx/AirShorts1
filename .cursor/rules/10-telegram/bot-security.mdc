# 🤖 Безопасность Telegram ботов

## 🚨 КРИТИЧЕСКИ ВАЖНО
**Telegram боты - это НЕ веб-приложения!** Не применяй стандартные веб-практики безопасности.

## ✅ Telegram сам обеспечивает автоматически:
- **Аутентификацию пользователей** - через Telegram ID
- **CSRF защиту** - нет веб-форм
- **XSS защиту** - сообщения изолированы
- **Rate limiting** - встроенный в API
- **Управление сессиями** - через Telegram

## 🔧 ОБЯЗАТЕЛЬНЫЕ МЕРЫ БЕЗОПАСНОСТИ:

### 1. Проверка прав доступа
```javascript
// ✅ Правильно - проверка администратора
const isAdmin = msg.from.id.toString() === process.env.ADMIN_USER_ID;

// ✅ Правильно - проверка роли
const userRole = await getUserRole(msg.from.id);
if (userRole !== 'admin') {
    return await ctx.reply('❌ Недостаточно прав');
}
```

### 2. Параметризованные SQL запросы
```javascript
// ✅ Правильно - параметризованный запрос
await db.query('SELECT * FROM users WHERE telegram_id = $1', [msg.from.id]);

// ❌ Неправильно - SQL инъекция
await db.query(`SELECT * FROM users WHERE telegram_id = ${msg.from.id}`);
```

### 3. Валидация входных данных
```javascript
// ✅ Правильно - валидация суммы
const amount = parseFloat(ctx.message.text);
if (isNaN(amount) || amount <= 0 || amount > 1000000) {
    return await ctx.reply('❌ Некорректная сумма');
}
```

### 4. Логирование действий
```javascript
// ✅ Правильно - логирование всех действий
logger.info(`[User ${msg.from.id}] Выполнил действие: ${action}`);
logger.warn(`[User ${msg.from.id}] Попытка доступа к админ функции`);
```

## 🚫 НЕ используй для Telegram ботов:

### 1. Веб-безопасность (не применимо):
- `SESSION_SECRET` - не нужен
- `JWT токены` - Telegram ID уже идентификатор
- `CSRF токены` - нет веб-форм
- `Пароли пользователей` - Telegram управляет аутентификацией
- `Веб-сессии` - Telegram управляет сессиями

### 2. Веб-заголовки (не применимо):
- `CSP заголовки` - нет веб-страниц
- `CORS` - нет кросс-доменных запросов
- `HTTPS` - Telegram API уже защищен

## 🔐 БЕЗОПАСНОЕ ХРАНЕНИЕ:

### 1. Переменные окружения
```env
# ✅ Обязательные для Telegram ботов
TELEGRAM_BOT_TOKEN=your_token
ADMIN_USER_ID=your_telegram_id
DATABASE_URL=postgresql://...

# ❌ НЕ нужны для Telegram ботов
# JWT_SECRET - Telegram сам обеспечивает безопасность
# SESSION_SECRET - не нужен
# CSRF_SECRET - не нужен
```

### 2. Безопасное хранение токенов
```javascript
// ✅ Правильно - через переменные окружения
const botToken = process.env.TELEGRAM_BOT_TOKEN;

// ❌ Неправильно - хардкод
const botToken = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz';
```

## 🛡️ ДОПОЛНИТЕЛЬНЫЕ МЕРЫ:

### 1. Rate limiting для пользователей
```javascript
// ✅ Ограничение частоты запросов
const userRequests = await redis.get(`rate_limit:${userId}`);
if (userRequests && parseInt(userRequests) > 10) {
    return await ctx.reply('⚠️ Слишком много запросов. Попробуйте позже.');
}
await redis.incr(`rate_limit:${userId}`);
await redis.expire(`rate_limit:${userId}`, 60); // 1 минута
```

### 2. Проверка подписи вебхуков (если используется)
```javascript
// ✅ Проверка подписи вебхука
const signature = req.headers['x-telegram-bot-api-signature'];
if (!verifyWebhookSignature(req.body, signature, botToken)) {
    return res.status(401).send('Unauthorized');
}
```

## 📋 ЧЕК-ЛИСТ БЕЗОПАСНОСТИ:
- [ ] Нет веб-практик безопасности
- [ ] Все SQL запросы параметризованы
- [ ] Проверка прав доступа реализована
- [ ] Валидация входных данных
- [ ] Логирование всех действий
- [ ] Токены в переменных окружения
- [ ] Rate limiting для пользователей
- [ ] Обработка ошибок без утечки данных

## 🚨 ЧАСТЫЕ ОШИБКИ:
1. **Применение веб-безопасности** к ботам
2. **Хардкод токенов** в коде
3. **Отсутствие валидации** входных данных
4. **SQL инъекции** через конкатенацию строк
5. **Игнорирование логирования** действий

---

**Помни: Telegram сам обеспечивает базовую безопасность - не дублируй веб-практики!**
**Фокус на бизнес-логике безопасности, а не на веб-безопасности!**
description:
globs:
alwaysApply: false
---
