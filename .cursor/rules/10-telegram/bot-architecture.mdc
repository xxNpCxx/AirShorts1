# 🏗️ Архитектура Telegram ботов

## 🎯 ОСНОВНЫЕ ПРИНЦИПЫ АРХИТЕКТУРЫ:

### 1. Модульная структура
```typescript
// ✅ Правильно - разделение по функциональности
src/
├── users/           # Управление пользователями
├── deals/           # Система сделок
├── referrals/       # Реферальная система
├── pricing/         # Ценообразование
├── settings/        # Настройки системы
├── analytics/       # Аналитика
└── shared/          # Общие утилиты
```

### 2. Разделение ответственности
```typescript
// ✅ Каждый модуль отвечает за свою область
@Module({
  imports: [DatabaseModule, RedisModule],
  providers: [UsersService, UsersRepository],
  controllers: [UsersController],
  exports: [UsersService]
})
export class UsersModule {}
```

### 3. Dependency Injection
```typescript
// ✅ Используй DI для слабой связанности
@Injectable()
export class UsersService {
  constructor(
    private readonly usersRepository: UsersRepository,
    private readonly logger: LoggerService
  ) {}
}
```

## 🔧 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:

### 1. Webhook vs Long Polling
```typescript
// ✅ Webhook для продакшена
if (process.env.NODE_ENV === 'production') {
  await bot.setWebhook(`${process.env.WEBHOOK_URL}/webhook`);
} else {
  // Long polling для разработки
  bot.launch();
}
```

### 2. Middleware порядок
```typescript
// ✅ Правильный порядок middleware
bot.use(session());           // 1. Сессии
bot.use(Stage.middleware());  // 2. FSM сцены
bot.use(adminMiddleware);     // 3. Проверка прав
bot.use(loggingMiddleware);   // 4. Логирование
```

### 3. Обработка команд
```typescript
// ✅ Один обработчик на команду
bot.command('start', handleStart);
bot.command('help', handleHelp);
bot.command('settings', handleSettings);

// ❌ НЕ регистрируй несколько обработчиков одной команды
```

## 📱 UX АРХИТЕКТУРА:

### 1. FSM (Finite State Machine)
```typescript
// ✅ Используй сцены для сложных диалогов
const exchangeScene = new Scene('exchange');
exchangeScene.enter((ctx) => {
  ctx.reply('💱 Выберите валюту:', currencyKeyboard);
});

exchangeScene.on('callback_query', async (ctx) => {
  // Обработка выбора валюты
});
```

### 2. Клавиатуры
```typescript
// ✅ Разделяй типы клавиатур
export class KeyboardService {
  // Inline клавиатуры для выбора действий
  createInlineKeyboard(buttons: Button[]): InlineKeyboardMarkup {
    return { inline_keyboard: buttons };
  }

  // Reply клавиатуры для навигации
  createReplyKeyboard(buttons: string[]): ReplyKeyboardMarkup {
    return { keyboard: buttons.map(text => [{ text }]) };
  }
}
```

### 3. Навигация
```typescript
// ✅ Простая и понятная навигация
export class NavigationService {
  async goToMainMenu(ctx: Context) {
    await ctx.reply('🏠 Главное меню:', mainMenuKeyboard);
  }

  async goBack(ctx: Context) {
    // Логика возврата назад
  }
}
```

## 🗄️ РАБОТА С ДАННЫМИ:

### 1. База данных
```typescript
// ✅ Используй репозитории
@Injectable()
export class UsersRepository {
  async findByTelegramId(telegramId: number): Promise<User> {
    return this.db.query(
      'SELECT * FROM users WHERE telegram_id = $1',
      [telegramId]
    );
  }
}
```

### 2. Кеширование
```typescript
// ✅ Кешируй часто используемые данные
@Injectable()
export class PricingService {
  async getCurrentPrice(currency: string): Promise<number> {
    const cached = await this.redis.get(`price:${currency}`);
    if (cached) return parseFloat(cached);

    const price = await this.fetchPrice(currency);
    await this.redis.setex(`price:${currency}`, 300, price.toString());
    return price;
  }
}
```

### 3. Транзакции
```typescript
// ✅ Используй транзакции для критических операций
async createDeal(userId: number, amount: number): Promise<Deal> {
  const client = await this.db.connect();
  try {
    await client.query('BEGIN');
    
    const deal = await this.dealsRepository.create(client, { userId, amount });
    await this.usersRepository.updateBalance(client, userId, -amount);
    
    await client.query('COMMIT');
    return deal;
  } catch (error) {
    await client.query('ROLLBACK');
    throw error;
  } finally {
    client.release();
  }
}
```

## 🔒 БЕЗОПАСНОСТЬ АРХИТЕКТУРЫ:

### 1. Валидация входных данных
```typescript
// ✅ Валидируй все входные данные
export class ValidationService {
  validateAmount(amount: string): number {
    const num = parseFloat(amount);
    if (isNaN(num) || num <= 0 || num > 1000000) {
      throw new Error('Некорректная сумма');
    }
    return num;
  }
}
```

### 2. Проверка прав доступа
```typescript
// ✅ Middleware для проверки прав
export const adminMiddleware = async (ctx: Context, next: NextFunction) => {
  const isAdmin = ctx.from?.id.toString() === process.env.ADMIN_USER_ID;
  if (!isAdmin) {
    return ctx.reply('❌ Недостаточно прав');
  }
  return next();
};
```

### 3. Логирование
```typescript
// ✅ Логируй все важные действия
export class LoggingService {
  logUserAction(userId: number, action: string, details?: any) {
    this.logger.info(`[User ${userId}] ${action}`, details);
  }

  logError(error: Error, context: string) {
    this.logger.error(`[${context}] Ошибка:`, error);
  }
}
```

## 📊 МОНИТОРИНГ И АНАЛИТИКА:

### 1. Метрики
```typescript
// ✅ Собирай метрики использования
export class MetricsService {
  async incrementMetric(name: string, value: number = 1) {
    await this.redis.incrby(`metric:${name}`, value);
  }

  async getMetric(name: string): Promise<number> {
    const value = await this.redis.get(`metric:${name}`);
    return value ? parseInt(value) : 0;
  }
}
```

### 2. Health checks
```typescript
// ✅ Проверяй состояние системы
@Controller('health')
export class HealthController {
  @Get()
  async check() {
    const dbStatus = await this.checkDatabase();
    const redisStatus = await this.checkRedis();
    
    return {
      status: dbStatus && redisStatus ? 'healthy' : 'unhealthy',
      timestamp: new Date().toISOString(),
      services: { database: dbStatus, redis: redisStatus }
    };
  }
}
```

## 📋 ЧЕК-ЛИСТ АРХИТЕКТУРЫ:
- [ ] Модульная структура
- [ ] Разделение ответственности
- [ ] Dependency Injection
- [ ] Правильный порядок middleware
- [ ] Один обработчик на команду
- [ ] FSM для сложных диалогов
- [ ] Разделение типов клавиатур
- [ ] Репозитории для работы с БД
- [ ] Кеширование частых данных
- [ ] Транзакции для критических операций
- [ ] Валидация входных данных
- [ ] Проверка прав доступа
- [ ] Логирование действий
- [ ] Метрики и мониторинг
- [ ] Health checks

---

**Помни: Хорошая архитектура - залог масштабируемости и поддерживаемости!**
description:
globs:
alwaysApply: false
---
